#FRONT END
    Produce build file: 
        ng build --configuration=production
    Move Folder to main directory
    Dockerfile for frontend: 
        docker build -f Dockerfile.front -t my-angular-app .
    Run container: 
        docker run -d -p 8080:80 my-angular-app

